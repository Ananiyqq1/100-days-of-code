### Problem Description
- Write a function that displays the alphabet in lowercase, on a single line, in ascending order, starting from the letter 'a' to the letter 'z'.

### Expected Function
```c
void print_alphabet(void);
```

### Allowed Functions
- write

### Usage
```c
#include <unistd.h>

int main(void)
{
    print_alphabet();
    return 0;
}
```

